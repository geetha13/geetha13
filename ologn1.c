#include <stdio.h>
#include <stdlib.h>
void print(int arr[],int size)
{
printf("repaeting elements are");
for(i=0;i<size;i++)
for(j=j+1;j<size;j++)
if(arr[i]=arr[j])
printf*"%D",arr[i])
printf("%d",arr[i])
}
int main()
{
int arr[]={4,1,4,6,3,1}
int arr_size=sizeof(arr)
printf(arr,arr_size);
getch();
return 0;
}
